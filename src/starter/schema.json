{
  "$schema": "http://json-schema.org/schema",
  "$id": "AngularStarterSchematics",
  "title": "Angular starter Schema",
  "type": "object",
  "description": "Generates Wiz boilerplate Angular starter application.",
  "properties": {
    "project": {
      "type": "string",
      "description": "Generate in specific Angular CLI workspace project"
    },
    "white-label": {
      "type": "boolean",
      "description": "Tells if the project is a 'white-label' project or not",
      "default": false
    }
  }
}

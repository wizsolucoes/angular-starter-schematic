{
  "$schema": "http://json-schema.org/schema",
  "id": "AngularStarterSchematics",
  "title": "Angular starter Schema",
  "type": "object",
  "description": "Generates Wiz boilerplate Angular starter application.",
  "properties": {
    "project": {
      "type": "string",
      "description": "Generate in specific Anuglar CLI workspace project"
    }
  }
}
